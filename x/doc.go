// Package x contains modules with external dependencies. These might have
// less strict compatibility guarantees, and it might not be possible to
// upgrade them atomically like we do for the core packages. In consequence,
// they are packaged as independent modules.
package x
